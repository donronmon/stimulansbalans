---
import logoSB from "../assets/svg/logoSB.svg?url";

// want to scale to the parent container
// predefined sizes
// include styling options, CSS? add a class via props?
// const {width, height} = Astro.props;
---

<svg>
    <use xlink:href={logoSB} />
</svg>